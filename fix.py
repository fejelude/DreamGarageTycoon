print("Ah! The user is saying: 'if a player is already crafting a car and tries to select another one... they shouldn't be able to proceed directly... The NPC should display dialogue... After 3 seconds, the Active Craft Frame appears.'")
print("This happens WHEN THEY SELECT A CAR FROM THE CHASSIS! That is `OpenCraftingMenu`!")
print("Currently, `AttemptCraft` handles this check when they click 'Craft'. But they shouldn't even be able to get to the 'Craft' button if they try to select another car while crafting!")
